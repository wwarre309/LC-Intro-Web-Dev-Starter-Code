// 11.3.3. Check Your Understanding
// Question: Use the map method to map an array of strings. For each name in the array, map it to the first initial.

let names = ["Chris", "Jim", "Sally", "Blake"];

// TODO: Write a mapping function
// and pass it to .map()
let firstInitials = names.map();

console.log(firstInitials);